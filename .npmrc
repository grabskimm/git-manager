# The UI pins Vite 8 while @vitejs/plugin-react's peer range trails behind it;
# the combination installs and builds fine, so accept it instead of failing the
# install. Applied repo-wide so local dev and CI resolve identically.
legacy-peer-deps=true
